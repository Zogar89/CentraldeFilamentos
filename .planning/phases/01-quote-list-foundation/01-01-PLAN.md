---
phase: 01-quote-list-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_frontend_assets.py
  - src/lib/quoteList.js
  - src/components/QuoteListPanel.svelte
  - src/CatalogApp.svelte
  - src/styles/global.css
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - ITEM-01
  - ITEM-02
  - PERS-01
  - DISC-01
must_haves:
  truths:
    - "Usuario puede agregar una presentacion desde el catalogo a Lista de cotizacion per D-05, D-06, D-07, D-26."
    - "Usuario ve la lista como herramienta de cotizacion, no carrito ni compra per D-04, D-27, D-29."
    - "La lista se guarda localmente con payload versionado usando product.id per D-10, D-11, D-12, D-13, D-30."
  artifacts:
    - path: "src/lib/quoteList.js"
      provides: "Persistencia versionada, snapshot y helpers base de lista"
      exports: ["quoteListStorageKey", "quoteListSchemaVersion", "loadQuoteList", "saveQuoteList", "normalizeQuoteList", "snapshotQuoteItem", "quoteQuantityLabel"]
    - path: "src/components/QuoteListPanel.svelte"
      provides: "Superficie visible inicial de Lista de cotizacion"
    - path: "src/CatalogApp.svelte"
      provides: "Integracion de +1 en presentation-row y activacion de lista"
    - path: "tests/test_frontend_assets.py"
      provides: "Invariantes de fuente para copia, almacenamiento, no-commerce y add control"
  key_links:
    - from: "src/CatalogApp.svelte"
      to: "src/lib/quoteList.js"
      via: "import de loadQuoteList/saveQuoteList/snapshotQuoteItem y mutaciones addQuoteItem"
      pattern: "quoteList"
    - from: "src/CatalogApp.svelte"
      to: "src/components/QuoteListPanel.svelte"
      via: "render condicional cuando quoteItems.length > 0"
      pattern: "QuoteListPanel"
---

<objective>
Crear el primer slice vertical de Phase 1: una persona agrega una presentacion desde el catalogo, aparece la Lista de cotizacion con framing no-commerce, y el estado queda autosave local versionado.

Purpose: habilitar la capacidad base de lista sin backend, provider selection, coverage, import/export ni WhatsApp.
Output: helper `src/lib/quoteList.js`, panel inicial, integracion `+1` por presentacion y pruebas de invariantes.
</objective>

## Phase Goal

**As a** maker que compra filamento en Argentina, **I want to** armar una lista local de cotizacion desde el catalogo, **so that** pueda preparar una consulta clara sin que StockCentral venda ni procese pedidos.

<execution_context>
@C:/Users/Gabriel/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Gabriel/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-quote-list-foundation/01-CONTEXT.md
@.planning/phases/01-quote-list-foundation/01-UI-SPEC.md
@.planning/phases/01-quote-list-foundation/01-RESEARCH.md
@.planning/phases/01-quote-list-foundation/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/STACK.md
@src/CatalogApp.svelte
@src/lib/stockSubscriptions.js
@src/lib/shared.js
@src/styles/global.css
@tests/test_frontend_assets.py
</context>

## Artifacts This Phase Produces

- `src/lib/quoteList.js`
- `src/components/QuoteListPanel.svelte`
- `src/components/QuoteListDrawer.svelte`
- `src/components/QuoteListItem.svelte`
- `src/components/QuoteQuantityControl.svelte`
- `quoteListStorageKey`
- `quoteListSchemaVersion`
- `loadQuoteList`
- `saveQuoteList`
- `normalizeQuoteList`
- `snapshotQuoteItem`
- `reconcileQuoteList`
- `clampQuoteQuantity`
- `quoteQuantityLabel`
- `QuoteListPanel`
- `QuoteListDrawer`
- `QuoteListItem`
- `QuoteQuantityControl`
- `addQuoteItem`
- `saveQuoteListState`
- `setQuoteItemQuantity`
- `removeQuoteItem`
- `clearQuoteList`
- `toggleQuoteControls`
- `openQuoteDrawer`
- `closeQuoteDrawer`
- `handleQuoteDrawerKeydown`
- `test_quote_list_source_contract_covers_foundation`
- `test_quote_list_styles_contract_covers_panel_and_controls`

## Source Audit Summary

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Armar lista local de cotizacion desde catalogo | 01-01, 01-02, 01-03 | COVERED | Primer slice + controles + resiliencia |
| REQ | LIST-01 | Agregar filamento desde catalogo | 01-01 | COVERED | `+1` en `.presentation-row` per D-05 |
| REQ | LIST-02 | Ver herramienta de cotizacion, no carrito | 01-01, 01-03 | COVERED | Copia e iconografia checklist/list per D-04 |
| REQ | ITEM-01 | Conservar material/color/marca/diametro/cantidad | 01-01, 01-02 | COVERED | Snapshot inicial y render de item |
| REQ | ITEM-02 | Conservar codigo cuando existe | 01-01, 01-02 | COVERED | SKU/EAN/original/article como snapshot, no key |
| REQ | PERS-01 | Autosave local | 01-01, 01-03 | COVERED | Versioned localStorage + fallback |
| REQ | DISC-01 | StockCentral no vende ni procesa pedidos | 01-01, 01-03 | COVERED | Notices bloqueados |
| RESEARCH | - | No new packages; no backend | 01-01 | COVERED | Browser-only Svelte/static |
| CONTEXT | D-01..D-33 | Locked decisions | 01-01, 01-02, 01-03 | COVERED | Cada tarea cita D-NN relevantes |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear invariantes fallidas del slice inicial</name>
  <files>tests/test_frontend_assets.py</files>
  <read_first>
    @tests/test_frontend_assets.py
    @.planning/phases/01-quote-list-foundation/01-CONTEXT.md
    @.planning/phases/01-quote-list-foundation/01-UI-SPEC.md
    @.planning/phases/01-quote-list-foundation/01-RESEARCH.md
    @.planning/phases/01-quote-list-foundation/01-PATTERNS.md
  </read_first>
  <behavior>
    - Test asserts `src/lib/quoteList.js`, `QuoteListPanel.svelte`, and `CatalogApp.svelte` contain the foundation identifiers.
    - Test asserts locked copy strings: `Lista de cotizacion`, `Usa esta lista para planificar tu compra. Confirma stock y precio final con cada proveedor.`, and `StockCentral no vende productos ni procesa pedidos.` per D-26, D-27, D-29.
    - Test asserts quote-list quantity source text uses `carrete` / `carretes` and not quote-list kg controls per D-18.
    - Test asserts banned commerce metaphors are absent from quote-list source and icon/control classes per D-04, D-29.
  </behavior>
  <action>Add `test_quote_list_source_contract_covers_foundation` to `tests/test_frontend_assets.py`, following the source aggregation analog in `01-PATTERNS.md` for `tests/test_frontend_assets.py` lines 605-685. Read all quote-list source files conditionally with `Path.exists()` so the first RED run reports missing artifacts clearly. Assert: `quoteListStorageKey`, `centraldefilamentos.quoteList.v1`, `quoteListSchemaVersion`, `loadQuoteList`, `saveQuoteList`, `normalizeQuoteList`, `snapshotQuoteItem`, `quoteQuantityLabel`, `QuoteListPanel`, `addQuoteItem`, `presentation-row`, `Agregar 1 carrete a la lista de cotizacion`, `+1`, `Lista de cotizacion`, main notice, non-commerce notice, and snapshot fields `productId`, `sku`, `ean`, `originalName`, `material`, `color`, `brand`, `diameterMm`, `presentation`, `quantity`. The negative assertions must inspect only quote-list files/classes where possible, not the entire app, because existing provider/footer copy may legitimately include non-list commerce-adjacent words. Run the test once and confirm it fails before implementation.</action>
  <verify>
    <automated>python -m pytest -v tests/test_frontend_assets.py::test_quote_list_source_contract_covers_foundation -x --basetemp C:\tmp\pytest-centraldefilamentos</automated>
  </verify>
  <acceptance_criteria>
    - The focused test exists and fails before production code is added.
    - The test references D-04, D-05, D-07, D-10, D-11, D-12, D-13, D-18, D-26, D-27, D-29, D-30 through asserted identifiers/copy.
    - No generated data file is edited.
  </acceptance_criteria>
  <done>RED test captures the foundation contract for LIST-01, LIST-02, ITEM-01, ITEM-02, PERS-01, and DISC-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar add +1 y panel inicial con autosave</name>
  <files>src/lib/quoteList.js, src/components/QuoteListPanel.svelte, src/CatalogApp.svelte, src/styles/global.css</files>
  <read_first>
    @src/lib/stockSubscriptions.js
    @src/lib/shared.js
    @src/CatalogApp.svelte
    @src/styles/global.css
    @.planning/phases/01-quote-list-foundation/01-CONTEXT.md
    @.planning/phases/01-quote-list-foundation/01-UI-SPEC.md
    @.planning/phases/01-quote-list-foundation/01-PATTERNS.md
  </read_first>
  <behavior>
    - Clicking `+1` on a product presentation adds 1 carrete per D-05, D-06, D-07, D-18, D-22.
    - Clicking `+1` again on the same `product.id` increments quantity by 1 per D-07, D-10.
    - Once item count is greater than 0, the list experience activates and desktop can render the panel per D-23.
    - Saved payload includes schema/version and item snapshot fields per D-10, D-11, D-12, D-13, D-30.
  </behavior>
  <action>Create `src/lib/quoteList.js` using plain JavaScript named exports, explicitly mirroring the storage-key/load/save/normalize analog from `src/lib/stockSubscriptions.js` documented in `01-PATTERNS.md` lines 212-246. Define `quoteListStorageKey = "centraldefilamentos.quoteList.v1"`, `quoteListSchemaVersion = 1`, `quoteQuantityLabel(quantity)`, `normalizeQuoteList(payload)`, `loadQuoteList()`, `saveQuoteList(state)`, and `snapshotQuoteItem(product, quantity)`. Use `product.id` as `productId`; store snapshot fields `productName`, `displayName`, `material`, `line`, `color`, `brand`, `diameterMm`, `presentation`, `sku`, `ean`, `articleCode`, `originalName`, `hasOnlineStock`, plus top-level integer `quantity`. Use helpers from `src/lib/shared.js` for display labels instead of duplicating formatter logic, per the shared formatter analog in `01-PATTERNS.md` lines 709-710. `loadQuoteList` and `saveQuoteList` must return status objects that can later expose storage failure, but this task only needs successful autosave path. Create `src/components/QuoteListPanel.svelte` with title `Lista de cotizacion`, count, main notice, non-commerce notice, local-only placeholder area, and a list of item names/quantities, following the prop/callback component analog from `src/components/SiteHeader.svelte`. In `src/CatalogApp.svelte`, import the helper and panel, add `quoteItems`, `quoteSettings`, `quoteStorageWarning` state, load after `products` are set, implement `addQuoteItem(product)` and `saveQuoteListState(nextItems, nextSettings = quoteSettings)`, following the existing `CatalogApp.svelte` import/state/onMount/reconcile/save analog documented in `01-PATTERNS.md` lines 22-174. Render a compact `+1` button in each `.presentation-row` header (not inside `.offer`) with aria-label `Agregar 1 carrete a la lista de cotizacion`, and render `QuoteListPanel` only when `quoteItems.length > 0`. Add only necessary CSS classes for the panel and `quote-add-button` using the exact `src/styles/global.css` responsive/layout analog from `01-PATTERNS.md` lines 445-600; before editing `src/styles/global.css`, inspect current file because it has pre-existing uncommitted changes and preserve unrelated user edits.</action>
  <verify>
    <automated>python -m pytest -v tests/test_frontend_assets.py::test_quote_list_source_contract_covers_foundation -x --basetemp C:\tmp\pytest-centraldefilamentos</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `+1` is rendered in every `.presentation-row` header and not inside provider `.offer` blocks per D-05, D-06, D-22.
    - Saved quote-list payload contains `schemaVersion: 1`, `items`, and `settings` per D-13.
    - Adding the same `product.id` increments item quantity by 1 per D-07 and D-10.
    - UI strings use `Lista de cotizacion` and `carrete/carretes`, not quote-list kg wording, per D-18 and D-26.
    - Build succeeds and focused test passes.
  </acceptance_criteria>
  <done>Usuario puede agregar una presentacion al listado y verla en una Lista de cotizacion autosave local.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser localStorage -> Svelte state | Saved JSON can be malformed or from an older schema. |
| Static stock.json -> quote snapshot | Public generated data is trusted as catalog input but can omit fields. |
| User click/input -> quote state | Add action mutates local saved state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01-01 | Tampering | `src/lib/quoteList.js` | mitigate | Validate payload shape and schema version before using saved state per D-13 and D-15. |
| T-01-01-02 | Spoofing | Quote-list copy/icons | mitigate | Use checklist/list wording and locked notices; avoid cart/checkout/order metaphors per D-04 and D-29. |
| T-01-01-03 | Information Disclosure | localStorage payload | accept | Payload stores only catalog item snapshot and quantities, no accounts or secrets. |
| T-01-01-04 | Denial of Service | localStorage exceptions | mitigate | This plan shapes status returns; Plan 03 surfaces fallback warning per D-32. |
</threat_model>

<verification>
Run:
- `python -m pytest -v tests/test_frontend_assets.py::test_quote_list_source_contract_covers_foundation -x --basetemp C:\tmp\pytest-centraldefilamentos`
- `npm run build`
</verification>

<success_criteria>
- LIST-01 add path works from presentation row.
- LIST-02 and DISC-01 no-commerce framing is visible.
- ITEM-01 and ITEM-02 snapshot fields are stored.
- PERS-01 autosave foundation uses versioned localStorage.
</success_criteria>

<output>
Create `.planning/phases/01-quote-list-foundation/01-01-SUMMARY.md` when done.
</output>

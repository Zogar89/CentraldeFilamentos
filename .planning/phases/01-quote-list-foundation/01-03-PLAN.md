---
phase: 01-quote-list-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - tests/test_frontend_assets.py
  - src/lib/quoteList.js
  - src/components/QuoteListPanel.svelte
  - src/components/QuoteListDrawer.svelte
  - src/CatalogApp.svelte
  - src/styles/global.css
autonomous: true
requirements:
  - LIST-02
  - ITEM-03
  - PERS-01
  - PERS-02
  - DISC-01
must_haves:
  truths:
    - "Desktop muestra panel derecho solo cuando hay items per D-01."
    - "Mobile muestra boton flotante checklist/list con contador y bottom drawer per D-02, D-03, D-04."
    - "localStorage incompatible/fallido se maneja con reset/fallback y aviso per D-14, D-15, D-28, D-32, D-33."
    - "Usuario ve local-only y no-commerce notices per D-27, D-28, D-29."
  artifacts:
    - path: "src/components/QuoteListDrawer.svelte"
      provides: "Bottom drawer mobile con close/Escape/backdrop"
    - path: "src/lib/quoteList.js"
      provides: "Reconciliacion y manejo de schema incompatible/storage failure"
      exports: ["reconcileQuoteList"]
    - path: "src/CatalogApp.svelte"
      provides: "Responsive activation and drawer state"
    - path: "src/styles/global.css"
      provides: "Panel desktop, floating button and drawer responsive styles"
    - path: "tests/test_frontend_assets.py"
      provides: "Invariantes de resiliencia y responsive surfaces"
  key_links:
    - from: "src/CatalogApp.svelte"
      to: "src/lib/quoteList.js"
      via: "reconcileQuoteList after stock.json loads"
      pattern: "reconcileQuoteList"
    - from: "src/CatalogApp.svelte"
      to: "src/components/QuoteListDrawer.svelte"
      via: "quoteDrawerOpen state and callbacks"
      pattern: "QuoteListDrawer"
---

<objective>
Cerrar Phase 1 con resiliencia y responsive: reconciliacion contra el catalogo publicado, fallback en memoria si storage falla, avisos local-only/no-commerce, panel desktop y drawer mobile.

Purpose: hacer confiable el flujo local en el browser y cumplir el contrato visual sin ampliar alcance a fases futuras.
Output: drawer mobile, reconciliacion, storage warnings, estilos responsive y pruebas finales.
</objective>

## Phase Goal

**As a** maker que arma una cotizacion local, **I want to** conservar y revisar mi lista en este navegador con una UI desktop/mobile clara, **so that** pueda seguir planificando aunque cambie el catalogo o falle el almacenamiento.

<execution_context>
@C:/Users/Gabriel/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Gabriel/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-quote-list-foundation/01-CONTEXT.md
@.planning/phases/01-quote-list-foundation/01-UI-SPEC.md
@.planning/phases/01-quote-list-foundation/01-RESEARCH.md
@.planning/phases/01-quote-list-foundation/01-PATTERNS.md
@.planning/phases/01-quote-list-foundation/01-02-SUMMARY.md
@src/lib/quoteList.js
@src/components/QuoteListPanel.svelte
@src/CatalogApp.svelte
@src/styles/global.css
@tests/test_frontend_assets.py
</context>

## Artifacts This Phase Produces

- `src/lib/quoteList.js`
- `src/components/QuoteListPanel.svelte`
- `src/components/QuoteListDrawer.svelte`
- `src/components/QuoteListItem.svelte`
- `src/components/QuoteQuantityControl.svelte`
- `quoteListStorageKey`
- `quoteListSchemaVersion`
- `loadQuoteList`
- `saveQuoteList`
- `normalizeQuoteList`
- `snapshotQuoteItem`
- `reconcileQuoteList`
- `clampQuoteQuantity`
- `quoteQuantityLabel`
- `QuoteListPanel`
- `QuoteListDrawer`
- `QuoteListItem`
- `QuoteQuantityControl`
- `addQuoteItem`
- `saveQuoteListState`
- `setQuoteItemQuantity`
- `removeQuoteItem`
- `clearQuoteList`
- `toggleQuoteControls`
- `openQuoteDrawer`
- `closeQuoteDrawer`
- `handleQuoteDrawerKeydown`
- `test_quote_list_source_contract_covers_foundation`
- `test_quote_list_styles_contract_covers_panel_and_controls`

## Source Audit Summary

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | LIST-02 | Cotizacion/no carrito framing | 01-03 | COVERED | Final desktop/mobile notices and checklist/list icon |
| REQ | ITEM-03 | Dato faltante/debe confirmarse | 01-03 | COVERED | Reconciliation and stock confirmation notices |
| REQ | PERS-01 | Autosave local robusto | 01-03 | COVERED | Save errors return warning and in-memory state |
| REQ | PERS-02 | Aviso local/no sync | 01-03 | COVERED | Locked local-only notice inside panel/drawer |
| REQ | DISC-01 | No vende/no procesa pedidos | 01-03 | COVERED | Locked non-commerce notice |
| RESEARCH | - | Drawer accessibility and localStorage exceptions | 01-03 | COVERED | Escape, close, backdrop, warning copy |
| CONTEXT | D-01..D-04, D-09, D-14, D-15, D-28, D-32, D-33 | Responsive/resilience decisions | 01-03 | COVERED | Esta plan cita e implementa cada decision |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agregar pruebas de responsive y resiliencia</name>
  <files>tests/test_frontend_assets.py</files>
  <read_first>
    @tests/test_frontend_assets.py
    @.planning/phases/01-quote-list-foundation/01-CONTEXT.md
    @.planning/phases/01-quote-list-foundation/01-UI-SPEC.md
    @.planning/phases/01-quote-list-foundation/01-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test asserts desktop side panel and mobile floating/drawer identifiers exist per D-01, D-02, D-03, D-04.
    - Test asserts reconciliation removes missing products and exposes count notice per D-14, D-33.
    - Test asserts incompatible schema/reset and storage warning copy exist per D-15, D-32.
    - Test asserts local-only notice is inside quote list surfaces per D-28 and PERS-02.
  </behavior>
  <action>Add `test_quote_list_styles_contract_covers_panel_and_controls` or extend it if already created, following the source and CSS invariant analogs from `tests/test_frontend_assets.py` in `01-PATTERNS.md` lines 605-685. Assert source identifiers and copy: `QuoteListDrawer`, `quote-floating-button`, `quote-list-drawer`, `quote-list-backdrop`, `quote-list-panel`, `quote-list-layout-active`, `openQuoteDrawer`, `closeQuoteDrawer`, `handleQuoteDrawerKeydown`, `reconcileQuoteList`, `removedCount`, `resetReason`, `storageAvailable`, `saveError`, `No pudimos guardar la lista en este navegador. La podes usar durante esta sesion, pero se puede perder al cerrar la pagina.`, `Se guarda solo en este navegador/dispositivo. No se sincroniza con otra PC o navegador.`, and `Quitamos {count} item(s) que ya no aparecen en el catalogo publicado.` Assert CSS includes `@media (max-width: 820px)`, `@media (max-width: 520px)`, `position: fixed`, `bottom:`, and desktop `minmax(320px, 360px)` or equivalent panel width. Run the focused test and confirm it fails before implementation.</action>
  <verify>
    <automated>python -m pytest -v tests/test_frontend_assets.py::test_quote_list_styles_contract_covers_panel_and_controls -x --basetemp C:\tmp\pytest-centraldefilamentos</automated>
  </verify>
  <acceptance_criteria>
    - Test fails before responsive/resilience implementation.
    - Test covers D-01, D-02, D-03, D-04, D-14, D-15, D-28, D-32, D-33.
    - Test avoids asserting global absence of words that can appear in non-quote legacy UI.
  </acceptance_criteria>
  <done>RED test captures final responsive and resilience contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar reconciliacion, fallback storage y responsive surfaces</name>
  <files>src/lib/quoteList.js, src/components/QuoteListPanel.svelte, src/components/QuoteListDrawer.svelte, src/CatalogApp.svelte, src/styles/global.css</files>
  <read_first>
    @src/lib/quoteList.js
    @src/components/QuoteListPanel.svelte
    @src/CatalogApp.svelte
    @src/styles/global.css
    @.planning/phases/01-quote-list-foundation/01-CONTEXT.md
    @.planning/phases/01-quote-list-foundation/01-UI-SPEC.md
    @.planning/phases/01-quote-list-foundation/01-PATTERNS.md
  </read_first>
  <behavior>
    - Saved items missing from current catalog are removed and a non-blocking count notice is shown per D-14 and D-33.
    - Incompatible schema resets/rejects controlled state per D-15.
    - localStorage unavailable or throwing keeps current-session memory list and shows warning per D-32.
    - Desktop side panel appears only when items exist per D-01.
    - Mobile floating checklist/list button with counter opens bottom drawer per D-02, D-03, D-04.
  </behavior>
  <action>In `src/lib/quoteList.js`, finish `loadQuoteList`, `saveQuoteList`, `normalizeQuoteList`, and add `reconcileQuoteList(items, products)`, explicitly extending the `src/lib/stockSubscriptions.js` load/save and `CatalogApp.svelte` `reconcileStockSubscriptions` analogs documented in `01-PATTERNS.md` lines 136-174 and 212-246. Return explicit fields `items`, `settings`, `storageAvailable`, `resetReason`, `saveError`, and `removedCount` where relevant. Unknown schema version must not be used; return empty items/settings with a controlled `resetReason` per D-15. Reconciliation must map `products` by `product.id`, refresh snapshots for found products through `snapshotQuoteItem`, drop missing products, and return `removedCount` per D-14. In `CatalogApp.svelte`, call `reconcileQuoteList` after stock data load and save reconciled state; close drawer when list becomes empty; preserve in-memory state if save returns failure, following the existing `onMount`, state mutation, and save analog in `01-PATTERNS.md` lines 22-174. Create `src/components/QuoteListDrawer.svelte` as a bottom drawer shell wrapping `QuoteListPanel`, with checklist/list icon button trigger in `CatalogApp.svelte`, `openQuoteDrawer`, `closeQuoteDrawer`, `handleQuoteDrawerKeydown`, close button, Escape close, and backdrop click. Use the `src/CatalogApp.svelte` image preview modal analog in `01-PATTERNS.md` lines 337-359 for conditional render, backdrop, close button and Escape handling, but add/omit modal semantics according to the resolved focus behavior in `01-RESEARCH.md`. Use checklist/list inline SVG only; do not use cart, bag, checkout, credit-card or order icon metaphors per D-04. Update `QuoteListPanel.svelte` so local-only notice appears below title and before item list per D-28, plus storage/reconciliation notices using polite live regions. Add CSS for `.quote-list-layout-active`, `.quote-list-panel`, `.quote-floating-button`, `.quote-list-drawer`, `.quote-list-backdrop`, and responsive rules using the exact `src/styles/global.css` analog in `01-PATTERNS.md` lines 445-600: desktop `> 960px` uses main catalog plus right panel width `minmax(320px, 360px)` only when items exist; `<= 820px` hides side panel and uses floating button/drawer; `<= 520px` wraps quantity controls into two rows. Inspect `src/styles/global.css` immediately before editing and preserve unrelated pre-existing uncommitted changes.</action>
  <verify>
    <automated>python -m pytest -v tests/test_frontend_assets.py -x --basetemp C:\tmp\pytest-centraldefilamentos</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Desktop side panel exists only while `quoteItems.length > 0` per D-01.
    - Mobile floating checklist/list button with count is hidden at zero items and opens bottom drawer per D-02 and D-03.
    - Storage warning copy appears when `localStorage` load/save fails, while current in-memory items remain usable per D-32.
    - Reconciliation removes missing `product.id` items and shows non-blocking removed count notice per D-14 and D-33.
    - Local-only and no-commerce notices appear inside panel/drawer per D-28 and DISC-01.
    - No import/export, provider coverage, WhatsApp message generation, backend persistence, checkout, ordering, pricing, or sales processing is introduced.
  </acceptance_criteria>
  <done>Phase 1 is complete: responsive local quote list is editable, autosaved, resilient and framed as cotizacion/no-commerce.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| localStorage -> quote state | Saved data can be malformed, stale or blocked by browser settings. |
| stock.json refresh -> saved product IDs | Products can disappear between captures. |
| Mobile drawer -> page content | Drawer interaction can leave background scroll/focus in an awkward state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-03-01 | Tampering | `loadQuoteList` / `normalizeQuoteList` | mitigate | Reject incompatible schema and malformed items with controlled reset per D-15. |
| T-01-03-02 | Denial of Service | localStorage failures | mitigate | Catch load/save failures, keep in-memory state, show warning per D-32. |
| T-01-03-03 | Repudiation | Reconciliation removal | mitigate | Show non-blocking removed-count notice inside panel/drawer per D-14 and D-33. |
| T-01-03-04 | Spoofing | Responsive list metaphor | mitigate | Checklist/list icon and no-commerce notices per D-04, D-27, D-28, D-29. |
| T-01-03-05 | Denial of Service | Mobile drawer focus/scroll | mitigate | Provide close button, Escape close, backdrop close, and avoid full-screen modal pattern per D-03. |
</threat_model>

<verification>
Run:
- `python -m pytest -v tests/test_frontend_assets.py -x --basetemp C:\tmp\pytest-centraldefilamentos`
- `npm run build`
</verification>

<success_criteria>
- Every Phase 1 requirement ID is implemented across the three plans.
- Every locked CONTEXT D-01 through D-33 is covered.
- `public/data/stock.json` remains read-only.
- No Phase 2-4 deferred feature is implemented.
</success_criteria>

<output>
Create `.planning/phases/01-quote-list-foundation/01-03-SUMMARY.md` when done.
</output>
